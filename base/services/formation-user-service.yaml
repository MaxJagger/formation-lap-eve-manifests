apiVersion: v1
kind: Service
metadata:
  name: formation-user
  namespace: formation-lap
spec:
  selector:
    app: formation-user
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP

